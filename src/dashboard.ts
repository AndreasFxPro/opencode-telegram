const html = `<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>OpenCode Mission Control</title>
  <link rel="stylesheet" href="/dashboard/style.css">
  <script src="/dashboard/app.js" defer></script>
</head>
<body>
  <header class="topbar">
    <div class="brand"><span class="brand-mark">◆</span><span>OPENCODE</span><b>MISSION CONTROL</b></div>
    <div class="top-stats" id="top-stats"></div>
    <div class="connection"><span class="pulse" id="pulse"></span><span id="connection">LOCKED</span></div>
  </header>
  <nav class="views" id="views" aria-label="Mission Control views"></nav>
  <div class="layout">
    <aside class="rail">
      <div class="rail-heading"><span id="rail-title">ATTENTION</span><span id="rail-count">0</span></div>
      <div class="rail-list" id="rail-list"></div>
    </aside>
    <main class="workspace" id="workspace"></main>
  </div>
  <div class="gate" id="gate">
    <form class="gate-card" id="gate-form">
      <div class="gate-icon">◆</div><div class="eyebrow">READ-ONLY HUB ACCESS</div><h1>Open Mission Control</h1>
      <p>Enter the dashboard token. It stays only in this tab's memory.</p>
      <input id="token" type="password" autocomplete="off" spellcheck="false" placeholder="oct_dash_..." required>
      <button type="submit">CONNECT <span>→</span></button><div class="gate-error" id="gate-error"></div>
    </form>
  </div>
</body>
</html>`

const css = `:root{color-scheme:dark;--bg:#090a0a;--panel:#101211;--panel2:#151715;--line:#252824;--muted:#777d75;--text:#e7e9e3;--amber:#f2a51a;--green:#71d08b;--red:#ef6a67;--blue:#75a7ff;--mono:"SFMono-Regular",Consolas,"Liberation Mono",monospace}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--text);font:13px/1.55 var(--mono);overflow:hidden}.topbar{height:54px;border-bottom:1px solid var(--line);display:flex;align-items:center;padding:0 18px;gap:24px;background:#0c0d0d}.brand{letter-spacing:.08em;display:flex;gap:8px;align-items:center;white-space:nowrap}.brand-mark{color:var(--amber)}.brand b{font-size:10px;color:var(--muted);font-weight:500}.top-stats{display:flex;gap:22px;margin-left:auto}.top-stat{display:flex;gap:7px;color:var(--muted)}.top-stat b{color:var(--text);font-weight:500}.connection{min-width:100px;text-align:right;color:var(--muted);font-size:11px}.pulse{display:inline-block;width:7px;height:7px;border-radius:50%;background:#555;margin-right:8px}.pulse.live{background:var(--green);box-shadow:0 0 10px #71d08b66}.views{height:42px;border-bottom:1px solid var(--line);display:flex;align-items:center;padding:0 18px;gap:4px;background:#0d0f0e}.view-button{height:30px;border:1px solid transparent;background:transparent;color:var(--muted);padding:0 12px;font:10px var(--mono);letter-spacing:.12em;cursor:pointer}.view-button:hover,.view-button:focus-visible{color:var(--text);border-color:var(--line);outline:0}.view-button.active{color:var(--amber);background:#171813;border-color:#313329}.badge{display:inline-block;min-width:18px;margin-left:7px;color:var(--text)}.layout{display:grid;grid-template-columns:310px 1fr;height:calc(100vh - 96px)}.rail{border-right:1px solid var(--line);background:#0d0f0e;min-height:0}.rail-heading,.panel-title{height:42px;padding:0 14px;display:flex;align-items:center;justify-content:space-between;border-bottom:1px solid var(--line);font-size:10px;letter-spacing:.14em;color:var(--muted)}.rail-list{overflow:auto;height:calc(100% - 42px)}.rail-card{width:100%;display:block;text-align:left;border:0;border-bottom:1px solid #1b1e1b;background:transparent;color:inherit;padding:13px 14px;cursor:pointer;font:inherit}.rail-card:hover,.rail-card:focus-visible{background:#131513;outline:0}.rail-card.active{background:#181a17;box-shadow:inset 2px 0 var(--amber)}.rail-top{display:flex;align-items:center;gap:8px}.rail-title{overflow:hidden;text-overflow:ellipsis;white-space:nowrap;flex:1}.rail-sub{font-size:10px;color:var(--muted);margin-top:4px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.dot{width:6px;height:6px;border-radius:50%;background:#555;flex:none}.dot.busy,.dot.pending{background:var(--amber)}.dot.online,.dot.completed{background:var(--green)}.dot.failed,.dot.blocked{background:var(--red)}.dot.running,.dot.review,.dot.verifying{background:var(--blue)}.workspace{overflow:auto;padding:28px}.page-head{display:flex;align-items:flex-start;justify-content:space-between;gap:20px;margin-bottom:22px}.eyebrow{font-size:10px;letter-spacing:.14em;color:var(--amber);text-transform:uppercase}.page-head h1,.empty h1{font-size:22px;font-weight:500;margin:5px 0 6px;letter-spacing:-.02em}.page-meta{color:var(--muted);font-size:11px}.status{border:1px solid var(--line);padding:5px 10px;font-size:10px;text-transform:uppercase;letter-spacing:.1em}.status.busy,.status.pending{color:var(--amber);border-color:#59451d}.status.failed,.status.blocked{color:var(--red);border-color:#57302e}.status.online,.status.completed{color:var(--green);border-color:#294c32}.status.running,.status.review,.status.verifying{color:var(--blue);border-color:#2d4162}.metrics{display:grid;grid-template-columns:repeat(6,1fr);border:1px solid var(--line);margin-bottom:18px}.metric{padding:13px;border-right:1px solid var(--line);min-width:0}.metric:last-child{border:0}.metric-label{font-size:9px;letter-spacing:.12em;color:var(--muted);margin-bottom:5px}.metric-value{font-size:16px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.grid{display:grid;grid-template-columns:minmax(0,1.55fr) minmax(280px,.85fr);gap:18px}.stack{display:flex;flex-direction:column;gap:18px}.panel{border:1px solid var(--line);background:var(--panel);min-width:0}.panel-body{padding:14px}.item{padding:12px 14px;border-bottom:1px solid #1e211e}.item:last-child{border-bottom:0}.item-head{display:flex;gap:10px;align-items:center}.item-title{flex:1;min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.item-meta{font-size:10px;color:var(--muted);white-space:nowrap}.item-detail{font-size:11px;color:#a7aba3;margin-top:6px;white-space:pre-wrap;overflow-wrap:anywhere}.description{color:#b7bbb3;white-space:pre-wrap;overflow-wrap:anywhere}.code{background:#0a0b0b;border:1px solid #20231f;padding:12px;white-space:pre-wrap;overflow-wrap:anywhere;color:#cfd2ca}.empty{height:100%;min-height:320px;display:flex;flex-direction:column;align-items:center;justify-content:center;text-align:center;color:var(--muted)}.empty-glyph{font-size:40px;color:#343832}.gate{position:fixed;inset:0;background:#090a0af5;display:flex;align-items:center;justify-content:center;z-index:10;padding:20px}.gate.hidden{display:none}.gate-card{width:min(420px,100%);border:1px solid var(--line);background:#101211;padding:30px}.gate-icon{color:var(--amber);font-size:26px}.gate-card h1{font-size:22px;font-weight:500;margin:10px 0}.gate-card p{color:var(--muted)}.gate-card input{width:100%;background:#090a0a;border:1px solid var(--line);color:var(--text);padding:12px;font:inherit;margin:12px 0}.gate-card button{width:100%;background:var(--amber);color:#171006;border:0;padding:12px;font:700 11px var(--mono);letter-spacing:.12em;cursor:pointer}.gate-error{color:var(--red);font-size:11px;margin-top:10px}.priority-urgent{color:var(--red)}.priority-high{color:var(--amber)}.muted{color:var(--muted)}@media(max-width:1050px){.metrics{grid-template-columns:repeat(3,1fr)}.metric:nth-child(3){border-right:0}.metric:nth-child(-n+3){border-bottom:1px solid var(--line)}}@media(max-width:900px){body{overflow:auto}.top-stats{display:none}.layout{display:block;height:auto}.rail{height:260px;border-right:0;border-bottom:1px solid var(--line)}.workspace{padding:18px;min-height:500px}.grid{grid-template-columns:1fr}.views{overflow:auto;padding:0 10px}.view-button{padding:0 9px}.page-head{align-items:flex-start}}@media(max-width:560px){.brand b{display:none}.topbar{padding:0 12px}.connection{margin-left:auto;min-width:80px}.metrics{grid-template-columns:repeat(2,1fr)}.metric,.metric:nth-child(3){border-right:1px solid var(--line);border-bottom:1px solid var(--line)}.metric:nth-child(2n){border-right:0}.metric:nth-last-child(-n+2){border-bottom:0}.workspace{padding:14px}.page-head{display:block}.status{display:inline-block;margin-top:10px}.rail{height:230px}}`

const responsiveCss = `.views{scrollbar-width:none}.views::-webkit-scrollbar{display:none}`

const js = `(()=>{
let token="",snapshot=null,view="overview",selected=null,timer=null;
const $=id=>document.getElementById(id);
const el=(tag,value,cls)=>{const node=document.createElement(tag);if(cls)node.className=cls;if(value!==undefined&&value!==null)node.textContent=String(value);return node};
const fmt=value=>new Intl.NumberFormat("en",{notation:Number(value)>9999?"compact":"standard",maximumFractionDigits:2}).format(Number(value)||0);
const age=value=>{const seconds=Math.max(0,Math.round((Date.now()-Number(value))/1000));return seconds<60?seconds+"s ago":seconds<3600?Math.floor(seconds/60)+"m ago":Math.floor(seconds/3600)+"h ago"};
const mission=()=>snapshot?.missionControl||{totals:{projects:0,activeWork:0,blocked:0,review:0,inbox:0},projects:[],workItems:[],inbox:[]};
const activeWork=()=>mission().workItems.filter(item=>item.state!=="completed"&&item.state!=="cancelled");
function stat(label,value){const node=el("div",null,"top-stat");node.append(el("span",label),el("b",value));return node}
function metric(label,value){const node=el("div",null,"metric");node.append(el("div",label,"metric-label"),el("div",value,"metric-value"));return node}
function dot(state){return el("span",null,"dot "+state)}
function item(title,meta,detail,state){const node=el("div",null,"item");const head=el("div",null,"item-head");head.append(dot(state||""),el("div",title,"item-title"),el("div",meta,"item-meta"));node.append(head);if(detail)node.append(el("div",detail,"item-detail"));return node}
function panel(title,count,items){const node=el("section",null,"panel");const heading=el("div",null,"panel-title");heading.append(el("span",title),el("span",count));node.append(heading);const body=el("div",null);if(items.length)for(const child of items)body.append(child);else body.append(el("div","Nothing here.","item muted"));node.append(body);return node}
function heading(eyebrow,title,meta,status){const node=el("div",null,"page-head");const copy=el("div");copy.append(el("div",eyebrow,"eyebrow"),el("h1",title),el("div",meta,"page-meta"));node.append(copy);if(status)node.append(el("span",status,"status "+status));return node}
function setSelected(items){if(!items.length){selected=null;return}if(!selected||!items.some(item=>item.key===selected||item.id===selected))selected=items[0].key||items[0].id}
function railCard(id,title,sub,state){const button=el("button",null,"rail-card"+(selected===id?" active":""));button.type="button";const top=el("div",null,"rail-top");top.append(dot(state),el("span",title,"rail-title"));button.append(top,el("div",sub,"rail-sub"));button.onclick=()=>{selected=id;render()};return button}
function renderTop(){const totals=snapshot.totals||{},control=mission().totals,top=$("top-stats");top.replaceChildren(stat("NODES",totals.connectedNodes+"/"+totals.nodes),stat("SESSIONS",totals.sessions),stat("ACTIVE",control.activeWork),stat("INBOX",control.inbox),stat("COST","$"+Number(totals.cost||0).toFixed(2)))}
function renderViews(){const definitions=[["overview","OVERVIEW",""],["projects","PROJECTS",mission().totals.projects],["queue","QUEUE",mission().totals.activeWork],["inbox","INBOX",mission().totals.inbox],["sessions","SESSIONS",snapshot.sessions.length]],nav=$("views");nav.replaceChildren();for(const [key,label,count] of definitions){const button=el("button",null,"view-button"+(view===key?" active":""));button.type="button";button.append(document.createTextNode(label));if(count!=="")button.append(el("span",count,"badge"));button.onclick=()=>{view=key;selected=null;render()};nav.append(button)}}
function renderRail(){let items=[],title="ATTENTION";if(view==="projects"){title="PROJECTS";items=mission().projects.map(project=>({id:project.id,title:project.name,sub:project.activeWork+" active · "+project.blockedWork+" blocked",state:project.blockedWork?"blocked":project.activeWork?"running":"online"}))}else if(view==="queue"){title="WORK QUEUE";items=activeWork().map(work=>({id:work.id,title:work.title,sub:work.projectName+" · "+work.priority,state:work.state}))}else if(view==="inbox"||view==="overview"){title="OPERATOR INBOX";items=mission().inbox.map(entry=>({id:entry.key,title:entry.title,sub:entry.project+" · "+entry.state,state:entry.state}))}else{title="SESSIONS";items=snapshot.sessions.map(s=>({id:s.key,title:s.title||s.sessionId,sub:s.nodeName+" · "+age(s.updatedAt),state:s.status==="busy"?"busy":s.connected?"online":""}))}setSelected(items.map(entry=>({key:entry.id})));$("rail-title").textContent=title;$("rail-count").textContent=String(items.length);const list=$("rail-list");list.replaceChildren();for(const entry of items)list.append(railCard(entry.id,entry.title,entry.sub,entry.state))}
function renderOverview(root){const control=mission(),totals=control.totals;root.append(heading("FLEET OPERATIONS","Mission Control",snapshot.totals.connectedNodes+" connected nodes · updated "+age(snapshot.generatedAt)));const metrics=el("div",null,"metrics");metrics.append(metric("PROJECTS",totals.projects),metric("ACTIVE WORK",totals.activeWork),metric("BLOCKED",totals.blocked),metric("IN REVIEW",totals.review),metric("INBOX",totals.inbox),metric("RUNNING SESSIONS",snapshot.totals.busy));root.append(metrics);const grid=el("div",null,"grid");const work=activeWork().slice(0,8).map(entry=>item(entry.title,entry.state+" · "+entry.priority,entry.projectName,entry.state));const projects=control.projects.slice(0,6).map(project=>item(project.name,project.activeWork+" active",project.description,project.blockedWork?"blocked":project.activeWork?"running":"online"));const attention=control.inbox.slice(0,8).map(entry=>item(entry.title,entry.state,entry.summary,entry.state));grid.append(panel("PRIORITY WORK",activeWork().length,work));const side=el("div",null,"stack");side.append(panel("ATTENTION",control.inbox.length,attention),panel("PROJECTS",control.projects.length,projects));grid.append(side);root.append(grid)}
function renderProject(root){const project=mission().projects.find(entry=>entry.id===selected);if(!project)return renderEmpty(root,"No project selected");root.append(heading(project.priority+" PRIORITY",project.name,project.key,project.blockedWork?"blocked":"online"));const metrics=el("div",null,"metrics");metrics.append(metric("ACTIVE",project.activeWork),metric("BLOCKED",project.blockedWork),metric("REVIEW",project.reviewWork),metric("SESSIONS",project.activeSessions),metric("VERSION",project.version),metric("UPDATED",age(project.updatedAt)));root.append(metrics);if(project.description)root.append(panel("BRIEF","",[el("div",project.description,"item description")]));const work=mission().workItems.filter(entry=>entry.projectId===project.id).map(entry=>item(entry.title,entry.state+" · "+entry.priority,entry.description,entry.state));root.append(panel("PROJECT WORK",work.length,work));if(project.repository)root.append(panel("REPOSITORY","",[el("div",project.repository,"item code")]))}
function renderWork(root){const work=mission().workItems.find(entry=>entry.id===selected);if(!work)return renderEmpty(root,"No work item selected");root.append(heading(work.projectName,work.title,work.id+" · updated "+age(work.updatedAt),work.state));const metrics=el("div",null,"metrics");metrics.append(metric("STATE",work.state),metric("PRIORITY",work.priority),metric("PROJECT",work.projectKey),metric("VERSION",work.version),metric("CREATED",age(work.createdAt)),metric("UPDATED",age(work.updatedAt)));root.append(metrics);root.append(panel("DESCRIPTION","",[el("div",work.description||"No description.","item description")]));root.append(panel("ACCEPTANCE CRITERIA","",[el("div",work.acceptance||"No acceptance criteria.","item code")]));if(work.sessionKey)root.append(panel("ATTACHED SESSION","",[el("div",work.sessionKey,"item code")]))}
function renderInbox(root){const entry=mission().inbox.find(item=>item.key===selected);if(!entry)return renderEmpty(root,"Inbox is clear");root.append(heading(entry.kind.replaceAll("_"," "),entry.title,entry.project+(entry.nodeName?" · "+entry.nodeName:""),entry.state));const metrics=el("div",null,"metrics");metrics.append(metric("STATE",entry.state),metric("KIND",entry.kind),metric("PROJECT",entry.project),metric("CREATED",age(entry.createdAt)),metric("UPDATED",age(entry.updatedAt)),metric("EXPIRES",entry.expiresAt?age(entry.expiresAt):"n/a"));root.append(metrics);root.append(panel("OPERATOR CONTEXT","",[el("div",entry.summary,"item description")]));root.append(panel("NEXT ACTION","",[el("div",entry.kind==="blocked_work"?"Update this work item from the hub CLI after resolving its blocker.":"Resolve this request in its exact Telegram approval message or in OpenCode.","item")]))}
function renderSession(root){const s=snapshot.sessions.find(entry=>entry.key===selected);if(!s)return renderEmpty(root,"No session selected");root.append(heading(s.nodeName+" / "+s.project,s.title||s.sessionId,s.sessionId+" · "+age(s.updatedAt),s.connected?s.status:"offline"));const t=s.tokens||{},metrics=el("div",null,"metrics");metrics.append(metric("TOKENS",fmt((t.input||0)+(t.output||0))),metric("INPUT",fmt(t.input)),metric("OUTPUT",fmt(t.output)),metric("CACHE READ",fmt(t.cacheRead)),metric("REASONING",fmt(t.reasoning)),metric("COST","$"+Number(s.cost||0).toFixed(4)));root.append(metrics);const activity=(s.activities||[]).slice().reverse().map(entry=>item(entry.title,entry.status||entry.type,entry.detail,entry.status==="error"?"failed":entry.status==="running"?"running":"online"));const todos=(s.todos||[]).map(todo=>item(todo.content,todo.priority||"",todo.status,todo.status==="completed"?"completed":todo.status==="in_progress"?"running":""));const grid=el("div",null,"grid");grid.append(panel("ACTIVITY",activity.length,activity));const side=el("div",null,"stack");side.append(panel("TODOS",todos.length,todos),panel("CONTEXT","",[el("div",["Agent: "+(s.agent||"unknown"),"Model: "+([s.provider,s.model].filter(Boolean).join("/")||"unknown"),"Capture: "+s.capture,...(s.directory?["Path: "+s.directory]:[])].join("\\n"),"item code")]));grid.append(side);root.append(grid)}
function renderEmpty(root,message){const node=el("section",null,"empty");node.append(el("div","⌁","empty-glyph"),el("h1",message),el("p","Mission Control will update as work and sessions arrive."));root.append(node)}
function render(){if(!snapshot)return;renderTop();renderViews();renderRail();const root=$("workspace");root.replaceChildren();if(view==="overview")renderOverview(root);else if(view==="projects")renderProject(root);else if(view==="queue")renderWork(root);else if(view==="inbox")renderInbox(root);else renderSession(root)}
async function load(){const response=await fetch("/v1/dashboard/snapshot",{headers:{authorization:"Bearer "+token},cache:"no-store"});if(!response.ok){if(response.status===401){token="";snapshot=null;selected=null;clearTimeout(timer);$("token").value="";$("token").focus();$("gate").classList.remove("hidden");$("gate-error").textContent="Dashboard token expired or was rotated";$("pulse").className="pulse";$("connection").textContent="LOCKED";return}throw new Error("Dashboard unavailable")}snapshot=await response.json();$("gate").classList.add("hidden");$("pulse").className="pulse live";$("connection").textContent="LIVE";render();clearTimeout(timer);timer=setTimeout(()=>load().catch(disconnected),3000)}
function disconnected(){$("pulse").className="pulse";$("connection").textContent="RETRYING";clearTimeout(timer);timer=setTimeout(()=>load().catch(disconnected),5000)}
$("gate-form").addEventListener("submit",event=>{event.preventDefault();token=$("token").value.trim();$("gate-error").textContent="";load().catch(error=>{$("gate-error").textContent=error.message})});
})();`

const headers = {
  "content-security-policy":
    "default-src 'none'; script-src 'self'; style-src 'self'; connect-src 'self'; base-uri 'none'; object-src 'none'; frame-ancestors 'none'; form-action 'none'",
  "cross-origin-resource-policy": "same-origin",
  "permissions-policy": "camera=(), microphone=(), geolocation=()",
  "referrer-policy": "no-referrer",
  "x-content-type-options": "nosniff",
  "x-frame-options": "DENY",
}

export function dashboardAsset(pathname: string) {
  if (pathname === "/dashboard" || pathname === "/dashboard/")
    return new Response(html, { headers: { ...headers, "content-type": "text/html; charset=utf-8" } })
  if (pathname === "/dashboard/style.css")
    return new Response(css + responsiveCss, { headers: { ...headers, "content-type": "text/css; charset=utf-8" } })
  if (pathname === "/dashboard/app.js")
    return new Response(js, { headers: { ...headers, "content-type": "text/javascript; charset=utf-8" } })
  return undefined
}

export function dashboardApiHeaders() {
  return {
    ...headers,
    "cache-control": "no-store",
    vary: "Authorization",
  }
}
